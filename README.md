# وب‌سایت شرکتی دادفر صنعت (پیشکار صنعت)

این پروژه یک وب‌سایت کامل شرکتی و کاتالوگ محصولات برای **شرکت مهندسی دادفر صنعت (پیشکار صنعت)** است که در زمینه فروش، تعمیر و نگهداری تخصصی کمپرسورهای صنعتی و تجهیزات جایگاه‌های CNG فعالیت می‌کند. این پروژه به عنوان یک نمونه کار (Portfolio) با استفاده از فریم‌ورک جنگو (Django) توسعه داده شده است.

![اسکرین‌شات پروژه](https://i.imgur.com/your-screenshot-link.png) 
*(نکته: یک اسکرین‌شات از سایت بگیرید، در سایتی مثل [Imgur](https://imgur.com/upload) آپلود کنید و لینک آن را جایگزین لینک بالا کنید)*

---

## 🚀 ویژگی‌ها و قابلیت‌ها (Features)

*   **طراحی مدرن و واکنش‌گرا (Responsive):** با الهام از وب‌سایت‌های صنعتی بزرگ دنیا (مانند Atlas Copco) و با استفاده از **Bootstrap 5**.
*   **صفحه اصلی داینامیک:** شامل بخش معرفی خدمات، آمار و افتخارات شرکت، و لیست محصولات اخیر.
*   **کاتالوگ محصولات:** قابلیت نمایش محصولات به همراه دسته‌بندی، تصویر، و توضیحات فنی.
*   **صفحه جزئیات محصول:** صفحه اختصاصی برای هر محصول با گالری تصاویر و مشخصات کامل.
*   **پنل مدیریت حرفه‌ای:** پنل ادمین کامل جنگو برای مدیریت آسان محصولات، خدمات، و درخواست‌ها توسط مدیر سایت.
*   **فرم تماس و درخواست خدمات:** فرمی برای ثبت درخواست تعمیرات یا مشاوره فنی.
*   **انیمیشن‌های جذاب:** استفاده از کتابخانه `AOS` برای ایجاد انیمیشن هنگام اسکرول.

---

## 🛠️ تکنولوژی‌های استفاده شده (Tech Stack)

*   **Backend:**
    *   **Python 3.11+**
    *   **Django 4.2+**
*   **Frontend:**
    *   **HTML5**
    *   **CSS3** (استایل‌های سفارشی)
    *   **Bootstrap 5.3** (برای ساختار واکنش‌گرا)
    *   **JavaScript** (برای افکت‌های داینامیک مانند منوی هوشمند)
*   **Database:**
    *   **SQLite 3** (برای حالت توسعه)
*   **Deployment:**
    *   آماده‌سازی برای اجرا روی سرور لینوکسی (Ubuntu) با `Gunicorn` و `Nginx`.

---

## ⚙️ راهنمای نصب و اجرا (Setup and Run)

برای اجرای این پروژه به صورت لوکال، مراحل زیر را دنبال کنید:

1.  **کلون کردن ریپازیتوری:**
    ```bash
    git clone https://github.com/SaeidAvani/Pishkar-sanat-website.git
    cd Pishkar-sanat-website
    ```

2.  **ساخت و فعال‌سازی محیط مجازی:**
    ```bash
    python -m venv venv
    source venv/bin/activate  # On Windows: venv\Scripts\activate
    ```

3.  **نصب نیازمندی‌ها:**
    ```bash
    pip install -r requirements.txt
    ```
    *(نکته: ابتدا با دستور `pip freeze > requirements.txt` فایل نیازمندی‌ها را در پروژه خودتان ایجاد کنید)*

4.  **اجرای Migration ها:**
    ```bash
    python manage.py migrate
    ```

5.  **ساخت Superuser (برای دسترسی به پنل ادمین):**
    ```bash
    python manage.py createsuperuser
    ```

6.  **اجرای سرور توسعه:**
    ```bash
    python manage.py runserver
    ```
    سایت روی آدرس `http://127.0.0.1:8000` در دسترس خواهد بود.

---

## 🖼️ گالری تصاویر

*(در این بخش می‌توانید چند اسکرین‌شات دیگر از صفحه محصولات یا پنل ادمین قرار دهید)*

---

This project was developed by **Saeid Avani** as a portfolio piece.
